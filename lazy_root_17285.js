(function(){  var out=[];  var counter = (window.__cyber_scope_counter || 0);  var all=document.querySelectorAll('*');  for(var i=0;i<all.length;i++){    var host=all[i];    var sr=host.shadowRoot;    if(sr){      var sheets=sr.__adoptedStyleSheets;      if(sheets && sheets.length){        var sid=host.getAttribute('__cyber_scope_id');        if(!sid){ sid='cs'+(++counter); }        var scopeClass='cyber-scope-'+sid;        for(var j=0;j<sheets.length;j++){          var css=sheets[j].cssText||'';          if(!css) continue;          var scoped=css.replace(/([^{};@][^{};]*)\{/g, function(m, sel){            if(sel.trim().charAt(0)==='@') return m;            var parts=sel.split(',');            for(var k=0;k<parts.length;k++){              var s=parts[k].trim();              if(!s) continue;              if(s.indexOf(':host')===0) s='.'+scopeClass+s.substring(5);              else s='.'+scopeClass+' '+s;              parts[k]=s;            }            return parts.join(', ')+'{';          });          out.push(scoped);        }      }    }  }  window.__cyber_scope_counter=counter;  return out;})()