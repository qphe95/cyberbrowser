# Static linking support for Windows distribution
if(CMAKE_SYSTEM_NAME STREQUAL "Windows" AND BGMDWNLDR_STATIC)
    set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -static -static-libgcc -static-libstdc++")
endif()
