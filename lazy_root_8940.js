if (typeof Intl === 'undefined') { var Intl = {}; }Intl.DateTimeFormat = function(locales, options) { this._locales = locales; this._options = options; };Intl.DateTimeFormat.prototype.format = function(date) {  if (!date) date = new Date();  return date.toLocaleString ? date.toLocaleString() : date.toString();};Intl.DateTimeFormat.prototype.resolvedOptions = function() {  return {locale: 'en-US', calendar: 'gregory', numberingSystem: 'latn', timeZone: 'UTC',          year: 'numeric', month: 'numeric', day: 'numeric', hour: 'numeric', minute: 'numeric', second: 'numeric'};};Intl.DateTimeFormat.supportedLocalesOf = function(locales) { return Array.isArray(locales) ? locales : ['en-US']; };Intl.NumberFormat = function(locales, options) { this._locales = locales; this._options = options; };Intl.NumberFormat.prototype.format = function(n) { return String(n); };Intl.NumberFormat.prototype.resolvedOptions = function() {  return {locale: 'en-US', numberingSystem: 'latn', style: 'decimal', minimumFractionDigits: 0, maximumFractionDigits: 3};};Intl.NumberFormat.supportedLocalesOf = function(locales) { return Array.isArray(locales) ? locales : ['en-US']; };