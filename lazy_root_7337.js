(function(){  if (typeof Node === 'undefined') return;  function toNodes(args) {    var out = [];    for (var i = 0; i < args.length; i++) {      var n = args[i];      if (n == null) continue;      if (typeof n === 'string' || typeof n === 'number') {        out.push(document.createTextNode(String(n)));      } else if (n instanceof DocumentFragment) {        while (n.firstChild) out.push(n.removeChild(n.firstChild));      } else if (n instanceof Node) {        out.push(n);      }    }    return out;  }  function insertNodes(parent, nodes, ref) {    for (var i = 0; i < nodes.length; i++) parent.insertBefore(nodes[i], ref);  }  var childProto = Element.prototype;  if (!childProto.before) {    childProto.before = function() {      var p = this.parentNode; if (!p) return; insertNodes(p, toNodes(arguments), this);    };  }  if (!childProto.after) {    childProto.after = function() {      var p = this.parentNode; if (!p) return; insertNodes(p, toNodes(arguments), this.nextSibling);    };  }  if (!childProto.replaceWith) {    childProto.replaceWith = function() {      var p = this.parentNode; if (!p) return;      var nodes = toNodes(arguments); insertNodes(p, nodes, this); p.removeChild(this);    };  }  if (!childProto.remove) {    childProto.remove = function() { if (this.parentNode) this.parentNode.removeChild(this); };  }  var parentProtos = [Document.prototype, DocumentFragment.prototype, Element.prototype];  if (typeof ShadowRoot !== 'undefined') parentProtos.push(ShadowRoot.prototype);  for (var i = 0; i < parentProtos.length; i++) {    var proto = parentProtos[i]; if (!proto) continue;    (function(p){      if (!p.append) p.append = function() {        var nodes = toNodes(arguments); for (var j = 0; j < nodes.length; j++) this.appendChild(nodes[j]);      };      if (!p.prepend) p.prepend = function() {        var nodes = toNodes(arguments); insertNodes(this, nodes, this.firstChild);      };    })(proto);  }})();